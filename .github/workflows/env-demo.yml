name: Env Demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: EnvDeMo
        run: echo "App is running in Env $APP_ENV"
