name: Input-Demo
on: 
  workflow_dispatch:
    inputs:
      enviroment:
        description: Please enter the Environment
        required: true
        default: dev
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Env
        run: echo "App is running in a {{ inputs.enviroment }}"

      
      
