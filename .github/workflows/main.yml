name: gha-script

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "Demo build"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test step
        run: echo "Demo Test"
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: deploy step
        run: echo "Demo deploy"
